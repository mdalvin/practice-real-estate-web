import React from 'react'
import styled, {css} from 'styled-components/macro'

const HerSection = styled.section`
    height: 100vh;
    max-height: 1100px;
    position: relative;
    overflow: hidden;
`;

const HeroWrapper = styled.div`
    width: 100%;
    height: 100%;
`;

const Hero = () => {
    return (
        <div>
            <h1>Hero here</h1>
        </div>
    )
}

export default Hero
